# Copyright (c) 2020, 2025, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

baseTypes:
- weblogic.management.runtime.WseeBaseOperationRuntimeMBean
descriptionHTML: '<p>Describes the state of a particular Web Service operation, such
  as deployment state and runtime statistics about the execution of the operation.</p>
  <p>This MBean can describe the operation on a web service client or service. Request
  statistics relate to outgoing requests on a client or incoming requests on a service.
  Response statistics relate to an incoming response on a client or an outgoing response
  on a service.</p> <p>Time values are reported in milliseconds</p> '
name: weblogic.management.runtime.WseeOperationRuntimeMBean
properties:
- defaultValue: {
    }
  descriptionHTML: 'Get attachment support for this port MBean. '
  name: PolicyAttachmentSupport
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'Get subject name for this port MBean. '
  name: PolicySubjectName
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'The policySubject parameter must uniquely identify what application,
    module, service, and port (port or operation for WLS Policy) is targeted. The
    syntax currently used by JRF (see [JRF ATTACHMENT POINT]) for J2EE Webservice
    Endpoints will be used: /{domain}/{instance}/{app}/WEBs|EJBs/{module }/WEBSERVICEs/{service}/PORTs/{port}/OPERATIONs/{operation} '
  name: PolicySubjectResourcePattern
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'Get subject type for this port MBean. '
  name: PolicySubjectType
  type: java.lang.String
