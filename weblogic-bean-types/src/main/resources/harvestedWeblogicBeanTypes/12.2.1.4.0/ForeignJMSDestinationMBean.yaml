# Copyright (c) 2020, 2023, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

actions: [
  ]
baseTypes:
- weblogic.management.configuration.ForeignJNDIObjectMBean
derivedTypes: [
  ]
descriptionHTML: 'This class represents a JMS destination that may be found on a remote
  server. The destination can represent a queue or a topic. When the destination is
  looked up on the local server, a look-up will be performed automatically on the
  remote JNDI directory, and the object will be returned from that directory. <p>This
  MBean will always be a sub-element of the ForeignJMSServer.</p> '
name: weblogic.management.configuration.ForeignJMSDestinationMBean
properties:
- defaultValue: {
    }
  descriptionHTML: ' '
  name: LocalJNDIName
  nullable: true
  type: java.lang.String
  writable: true
- defaultValue: {
    }
  descriptionHTML: ' '
  name: RemoteJNDIName
  nullable: true
  type: java.lang.String
  writable: true
