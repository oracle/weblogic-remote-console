# Copyright (c) 2020, 2023, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

actions: [
  ]
baseTypes:
- weblogic.management.runtime.TaskRuntimeMBean
derivedTypes:
- weblogic.management.runtime.RolloutTaskRuntimeMBean
descriptionHTML: 'TaskRuntimeMBean for any workflow (WorkflowProgress) executed using
  management orchestration framework. '
name: weblogic.management.runtime.WorkflowTaskRuntimeMBean
properties:
- array: true
  defaultValue: {
    }
  descriptionHTML: 'Lists all errors in the workflow. '
  name: Errors
  type: java.lang.Exception
- defaultValue: {
    }
  descriptionHTML: 'Number of completed commands in the workflow. Together with <code>getNumTotalCommands()</code>
    defines kind of progress information. '
  name: NumCompletedCommands
  type: int
- defaultValue: {
    }
  descriptionHTML: 'Number of all commands in the workflow. Together with <code>getNumCompletedCommands()</code>
    defines kind of progress information. '
  name: NumTotalCommands
  type: int
- defaultValue: {
    }
  descriptionHTML: 'Human-readable message containing information about the current
    workflow progress. '
  name: ProgressString
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'Provides step by step information (log) about the progress of
    the workflow. '
  name: StatusHistory
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'Each workflow has a unique id. '
  name: WorkflowId
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'User defined name of this workflow. '
  name: WorkflowName
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'Returns the target that was specified when the workflow was created.
    It will typically be either the name of the domain, of one or more clusters, or
    one or more servers. '
  name: WorkflowTarget
  type: java.lang.String
- defaultValue: {
    }
  descriptionHTML: 'Returns a string that can be used to identify the different types
    of workflows. '
  name: WorkflowType
  type: java.lang.String
