# Copyright (c) 2020, 2023, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

actions: [
  ]
baseTypes:
- weblogic.management.runtime.RuntimeMBean
derivedTypes: [
  ]
descriptionHTML: '<p>Encapsulates runtime information about WS-RM functionality. If
  this MBean is parented by a WseePortRuntimeMBean instance, this MBean represents
  WS-RM resources contained in a particular Web Service or web service client. If
  this MBean is parented by ServerRuntimeMBean, this MBean represents WS-RM resources
  for the entire server/VM (spanning across applications, web services, clients, etc.)</p> '
name: weblogic.management.runtime.WseeWsrmRuntimeMBean
properties:
- array: true
  defaultValue: {
    }
  descriptionHTML: '<p>A list of sequence IDs representing active sequences being
    managed on the WS-RM destination (receiving side).</p> '
  name: SequenceIds
  type: java.lang.String
