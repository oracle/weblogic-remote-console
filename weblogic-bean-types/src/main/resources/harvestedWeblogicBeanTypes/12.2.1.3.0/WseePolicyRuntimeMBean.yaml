# Copyright (c) 2020, 2023, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

actions: [
  ]
baseTypes:
- weblogic.management.runtime.RuntimeMBean
derivedTypes: [
  ]
descriptionHTML: '<p>Specifies runtime information about the WS-Policy files that
  are available for the Web Service.</p>  <p>Web Services can have one or more WS-Policy
  files associated with the endpoint itself or its operations; the WS-Policy files
  specify configuration options for Web Service message-level security (encryption
  and digital signatures) and reliable SOAP messaging.</p>  <p>Programmers can use
  the @Policy JWS annotation to associate a WS-Policy file to a Web Service at development
  time.  Administrators can also associate additional WS-Policy files at runtime.</p> '
name: weblogic.management.runtime.WseePolicyRuntimeMBean
properties:
- array: true
  defaultValue: {
    }
  descriptionHTML: ' '
  name: AvailablePolicies
  type: java.lang.String
