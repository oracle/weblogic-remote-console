import { By } from 'selenium-webdriver';
import { DriverLike } from '../../';
import { OjButtonsetMany } from './OjButtonsetMany';
export { OjButtonsetMany };
/**
 * Retrieve an instance of [OjButtonsetMany](../classes/elements.OjButtonsetMany.html).
 * @example
 * ```javascript
 * import { ojButtonsetMany } from '@oracle/oraclejet-webdriver/elements';
 * const el = await ojButtonsetMany(driver, By.id('my-oj-buttonset-many'));
 * ```
 * @param driver A WebDriver/WebElement instance from where the element will be
 * searched. If WebDriver is passed, the element will be searched globally in the
 * document. If WebElement is passed, the search will be relative to this element.
 * @param by The locator with which to find the element
 */
export declare function ojButtonsetMany(driver: DriverLike, by: By): Promise<OjButtonsetMany>;
