import { By } from 'selenium-webdriver';
import { DriverLike } from '../../';
import { OjColorPalette } from './OjColorPalette';
export { OjColorPalette };
/**
 * Retrieve an instance of [OjColorPalette](../classes/elements.OjColorPalette.html).
 * @example
 * ```javascript
 * import { ojColorPalette } from '@oracle/oraclejet-webdriver/elements';
 * const el = await ojColorPalette(driver, By.id('my-oj-color-palette'));
 * ```
 * @param driver A WebDriver/WebElement instance from where the element will be
 * searched. If WebDriver is passed, the element will be searched globally in the
 * document. If WebElement is passed, the search will be relative to this element.
 * @param by The locator with which to find the element
 */
export declare function ojColorPalette(driver: DriverLike, by: By): Promise<OjColorPalette>;
