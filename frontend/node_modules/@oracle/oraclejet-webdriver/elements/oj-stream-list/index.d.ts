import { By } from 'selenium-webdriver';
import { DriverLike } from '../../';
import { OjStreamList } from './OjStreamList';
export { OjStreamList };
/**
 * Retrieve an instance of [OjStreamList](../classes/elements.OjStreamList.html).
 * @example
 * ```javascript
 * import { ojStreamList } from '@oracle/oraclejet-webdriver/elements';
 * const el = await ojStreamList(driver, By.id('my-oj-stream-list'));
 * ```
 * @param driver A WebDriver/WebElement instance from where the element will be
 * searched. If WebDriver is passed, the element will be searched globally in the
 * document. If WebElement is passed, the search will be relative to this element.
 * @param by The locator with which to find the element
 */
export declare function ojStreamList(driver: DriverLike, by: By): Promise<OjStreamList>;
