import { By } from 'selenium-webdriver';
import { DriverLike } from '../../';
import { OjDrawerPopup } from './OjDrawerPopup';
export { OjDrawerPopup };
/**
 * Retrieve an instance of [OjDrawerPopup](../classes/elements.OjDrawerPopup.html).
 * @example
 * ```javascript
 * import { ojDrawerPopup } from '@oracle/oraclejet-webdriver/elements';
 * const el = await ojDrawerPopup(driver, By.id('my-oj-drawer-popup'));
 * ```
 * @param driver A WebDriver/WebElement instance from where the element will be
 * searched. If WebDriver is passed, the element will be searched globally in the
 * document. If WebElement is passed, the search will be relative to this element.
 * @param by The locator with which to find the element
 */
export declare function ojDrawerPopup(driver: DriverLike, by: By): Promise<OjDrawerPopup>;
