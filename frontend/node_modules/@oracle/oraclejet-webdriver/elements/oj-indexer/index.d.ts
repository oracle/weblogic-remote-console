import { By } from 'selenium-webdriver';
import { DriverLike } from '../../';
import { OjIndexer } from './OjIndexer';
export { OjIndexer };
/**
 * Retrieve an instance of [OjIndexer](../classes/elements.OjIndexer.html).
 * @example
 * ```javascript
 * import { ojIndexer } from '@oracle/oraclejet-webdriver/elements';
 * const el = await ojIndexer(driver, By.id('my-oj-indexer'));
 * ```
 * @param driver A WebDriver/WebElement instance from where the element will be
 * searched. If WebDriver is passed, the element will be searched globally in the
 * document. If WebElement is passed, the search will be relative to this element.
 * @param by The locator with which to find the element
 */
export declare function ojIndexer(driver: DriverLike, by: By): Promise<OjIndexer>;
