import { By } from 'selenium-webdriver';
import { DriverLike } from '../../';
import { OjMenuButton } from './OjMenuButton';
export { OjMenuButton };
/**
 * Retrieve an instance of [OjMenuButton](../classes/elements.OjMenuButton.html).
 * @example
 * ```javascript
 * import { ojMenuButton } from '@oracle/oraclejet-webdriver/elements';
 * const el = await ojMenuButton(driver, By.id('my-oj-menu-button'));
 * ```
 * @param driver A WebDriver/WebElement instance from where the element will be
 * searched. If WebDriver is passed, the element will be searched globally in the
 * document. If WebElement is passed, the search will be relative to this element.
 * @param by The locator with which to find the element
 */
export declare function ojMenuButton(driver: DriverLike, by: By): Promise<OjMenuButton>;
