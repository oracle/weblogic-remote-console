/* @oracle/oraclejet-preact: undefined */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var UNSAFE_TestAdapter = require('@oracle/oraclejet-testing/UNSAFE_TestAdapter');
var UNSAFE_Driver = require('@oracle/oraclejet-testing/UNSAFE_Driver');

class DatePickerAdapter extends UNSAFE_TestAdapter.TestAdapter {
    /**
     * Change and commit the value of the component.
     * @param value {CalendarDateRequired} The new value.
     * @returns Promise<void>
     */
    async changeValue(value) {
        const driver = this.getTestDriver();
        const el = await this.getElement();
        // executeScript can only consume local variables.
        const newValue = value;
        // For now, unwrap the TestElement to get the underlying DOM element until JET-73740 is fixed.
        await driver.executeScript(
        /* eslint-disable-next-line @typescript-eslint/no-explicit-any */
        (el, newValue) => el._changeValue(newValue), UNSAFE_Driver.unwrapElement(el), newValue);
    }
}

exports.DatePickerAdapter = DatePickerAdapter;
//# sourceMappingURL=DatePickerAdapter.js.map
