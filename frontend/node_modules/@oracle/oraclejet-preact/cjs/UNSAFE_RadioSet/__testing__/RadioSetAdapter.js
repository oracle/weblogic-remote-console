/* @oracle/oraclejet-preact: undefined */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var UNSAFE_Locators = require('@oracle/oraclejet-testing/UNSAFE_Locators');
var UNSAFE_TestAdapter = require('@oracle/oraclejet-testing/UNSAFE_TestAdapter');

class RadioSetAdapter extends UNSAFE_TestAdapter.TestAdapter {
    /**
     * Change the value of the component.
     * @param value {string | number} The new value.
     * @returns Promise<void>
     */
    async changeValue(value) {
        const driver = this.getTestDriver();
        const el = await this.getElement();
        const labels = await el.waitForElements(UNSAFE_Locators.byCss(`div[role="radiogroup"] label:not([for])`));
        await Promise.all(labels.map(async (label) => {
            const input = await label.waitForElement(UNSAFE_Locators.byCss('input[type="radio"]'));
            const inputValue = await input.getAttribute('value');
            if (value == inputValue) {
                await driver.click(label);
            }
        }));
    }
}

exports.RadioSetAdapter = RadioSetAdapter;
//# sourceMappingURL=RadioSetAdapter.js.map
