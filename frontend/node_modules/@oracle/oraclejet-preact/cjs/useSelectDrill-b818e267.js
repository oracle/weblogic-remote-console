/* @oracle/oraclejet-preact: undefined */
'use strict';

var mergeProps = require('./mergeProps-e3da7237.js');
var useChartDataCursor = require('./useChartDataCursor-363a537d.js');
var useChartDatatip = require('./useChartDatatip-76d4aeaa.js');
var eventsUtils = require('./eventsUtils-d34f8647.js');
require('./accUtils-5c7d3255.js');
var useChartDrill = require('./useChartDrill-8ada5349.js');
var useSelection = require('./useSelection-b5b55bdd.js');

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * Licensed under The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
function useChartDatatipAndCursor({ series, leafGroupsInfo, width, height, findNearest, getMarkerInfo, onDataCursorPositionChange, isDataCursorEnabled, dataCursorPosition, dataCursorStyle, hoveredItemInfo, focusedItemInfo, getDataItem, getDataItemPos, datatip, valueFormats, isHoriz, xScale, yScale, y2Scale, plotAreaSpace, timeAxisType, touchResponse, isDisabled, defaultXFormatter, defaultYFormatter, defaultY2Formatter, hasXYValues, getGroupFromPath, translations }) {
    const { datatipContent, datatipProps } = useChartDatatip.useChartDatatip({
        series,
        leafGroupsInfo,
        width,
        height,
        isDataCursorEnabled,
        dataCursorPosition,
        hoveredItemInfo,
        getDataItem,
        getDataItemPos,
        focusedItemInfo,
        datatip,
        valueFormats,
        isHoriz,
        xScale,
        findNearest,
        timeAxisType,
        touchResponse,
        isDisabled,
        defaultXFormatter,
        defaultYFormatter,
        defaultY2Formatter,
        hasXYValues,
        translations,
        getGroupFromPath
    });
    const { dataCursorContent, dataCursorProps } = useChartDataCursor.useChartDataCursor({
        touchResponse,
        findNearest,
        dataCursorPosition,
        onDataCursorPositionChange,
        isDataCursorEnabled,
        dataCursorStyle,
        isHoriz,
        getMarkerInfo,
        focusedItemInfo,
        plotAreaSpace,
        yScale,
        y2Scale,
        xScale,
        getDataItemPos
    });
    return {
        datatipContent,
        dataCursorContent,
        datatipProps: mergeProps.mergeProps(dataCursorProps, datatipProps)
    };
}

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * Licensed under The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
function useSelectDrill({ selectionMode, selection, onChange, rootRef, drilling, onItemDrill, onGroupDrill, focusedGroupInfo, focusedItemInfo, levelsArray, getDataItem, getDataById, ignoreAsTrigger }) {
    const idExtracter = (event) => {
        const isItemFocused = focusedItemInfo.isCurrent;
        const info = isItemFocused && event.type === 'keyup'
            ? focusedItemInfo
            : eventsUtils.getInfo(rootRef, event.target);
        return info && info.seriesIndex != null && info.groupIndex != null
            ? getDataItem(info.seriesIndex, info.groupIndex, levelsArray[levelsArray.length - 1][info.groupIndex].path)?.id
            : undefined;
    };
    const { selectionProps, selectionContent } = useSelection.useSelection({
        idExtracter,
        isDrillEnabled: drilling === 'on',
        selection,
        selectionMode,
        onChange,
        getDataById,
        ignoreAsTrigger
    });
    const drillProps = useChartDrill.useChartDrill({
        rootRef,
        selectionMode,
        drilling,
        focusedItemInfo,
        focusedGroupInfo,
        levelsArray,
        getDataItem,
        onItemDrill,
        onGroupDrill
    });
    return { selectDrillProps: mergeProps.mergeProps(selectionProps, drillProps), selectionContent };
}

exports.useChartDatatipAndCursor = useChartDatatipAndCursor;
exports.useSelectDrill = useSelectDrill;
//# sourceMappingURL=useSelectDrill-b818e267.js.map
