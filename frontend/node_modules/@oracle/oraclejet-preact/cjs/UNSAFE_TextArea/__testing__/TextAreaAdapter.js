/* @oracle/oraclejet-preact: undefined */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var UNSAFE_TestAdapter = require('@oracle/oraclejet-testing/UNSAFE_TestAdapter');
var UNSAFE_Driver = require('@oracle/oraclejet-testing/UNSAFE_Driver');
var UNSAFE_Locators = require('@oracle/oraclejet-testing/UNSAFE_Locators');

class TextAreaAdapter extends UNSAFE_TestAdapter.TestAdapter {
    /**
     * Clear and commit the value of the textarea element
     * @returns Promise<void>
     */
    async clear() {
        await this.changeValue(undefined);
    }
    /**
     * Change and commit the value of the textarea
     * @param value {string | undefined}  The new value
     * @returns Promise<void>
     */
    async changeValue(value) {
        const driver = this.getTestDriver();
        const el = await this.getElement();
        // Find the <textarea> element underneath the root.
        const textarea = await el.waitForElement(UNSAFE_Locators.byCss('textarea'));
        // Clear any existing value.
        await driver.sendKeys(textarea, UNSAFE_Driver.Keys['CONTROL_COMMAND'], 'a');
        await driver.sendKeys(textarea, UNSAFE_Driver.Keys['DELETE']);
        // Type the value into <textarea>.
        if (value !== undefined) {
            await driver.sendKeys(textarea, value);
        }
        // Commit.
        await driver.sendKeys(textarea, UNSAFE_Driver.Keys['TAB']);
    }
}

exports.TextAreaAdapter = TextAreaAdapter;
//# sourceMappingURL=TextAreaAdapter.js.map
