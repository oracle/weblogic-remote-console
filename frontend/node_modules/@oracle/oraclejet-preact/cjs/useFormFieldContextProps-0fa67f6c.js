/* @oracle/oraclejet-preact: undefined */
'use strict';

var hooks = require('preact/hooks');

/**
 * A custom hook to determine props to set FormFieldContext
 */
function useFormFieldContextProps({ hasTruncatedLabel, hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly }) {
    return hooks.useMemo(() => {
        return isReadonly
            ? {
                hasTruncatedLabel,
                hasValue,
                isFocused,
                isInputFocused,
                isLoading,
                isReadonly: true
            }
            : isDisabled
                ? {
                    hasTruncatedLabel: false,
                    hasValue,
                    isDisabled: true,
                    isLoading
                }
                : {
                    hasTruncatedLabel,
                    hasValue,
                    isFocused,
                    isInputFocused,
                    isLoading
                };
    }, [hasTruncatedLabel, hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly]);
}

exports.useFormFieldContextProps = useFormFieldContextProps;
//# sourceMappingURL=useFormFieldContextProps-0fa67f6c.js.map
