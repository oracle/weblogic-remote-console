/* @oracle/oraclejet-preact: undefined */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
var compat = require('preact/compat');
var useBareButton = require('./useBareButton-8c4f3b41.js');
var unsafeDomAccess = require('./unsafeDomAccess-c3a492f0.js');

/**
 * BareButtons are unformatted bare buttons that direct users to initiate or take actions and work with a single tap, click, or keystroke.
 */
const BareButton = compat.forwardRef((props, ref) => {
    const rootRef = compat.useRef(null);
    compat.useImperativeHandle(ref, () => {
        return {
            focus: () => {
                rootRef.current?.focus();
            },
            blur: () => {
                rootRef.current?.blur();
            },
            click: () => {
                rootRef.current?.click();
            },
            [unsafeDomAccess.UNSAFE_DOM_ACCESS]: rootRef.current
        };
    }, []);
    const { style: buttonStyle, classNames: buttonClass, ...rest } = { ...props };
    const buttonProps = useBareButton.useBareButton({ ...rest });
    const ElementType = props.elementDetails ? props.elementDetails.type : 'button';
    return (jsxRuntime.jsx(ElementType, { ref: rootRef, style: buttonStyle, class: buttonClass, ...buttonProps, children: props.children }));
});
BareButton.displayName = 'BareButton';

exports.BareButton = BareButton;
//# sourceMappingURL=BareButton-814ae4b7.js.map
