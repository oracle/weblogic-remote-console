/* @oracle/oraclejet-preact: undefined */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var UNSAFE_Locators = require('@oracle/oraclejet-testing/UNSAFE_Locators');
var UNSAFE_TestAdapter = require('@oracle/oraclejet-testing/UNSAFE_TestAdapter');

class CheckboxAdapter extends UNSAFE_TestAdapter.TestAdapter {
    /**
     * Change the value of the component.
     * @param value {boolean | undefined} The new value.
     * @returns Promise<void>
     */
    async changeValue(value) {
        const driver = this.getTestDriver();
        const el = await this.getElement();
        const label = await el.waitForElement(UNSAFE_Locators.byCss('label'));
        const input = await label.waitForElement(UNSAFE_Locators.byCss('input[type="checkbox"]'));
        const isChecked = (await input.getAttribute('aria-checked')) === 'true';
        const cVal = value ?? false;
        if (isChecked !== cVal) {
            await driver.click(label);
        }
    }
    /**
     * Clear the value of the component.
     * @returns Promise<void>
     */
    async clear() {
        await this.changeValue(undefined);
    }
}

exports.CheckboxAdapter = CheckboxAdapter;
//# sourceMappingURL=CheckboxAdapter.js.map
