import { TestAdapter } from '@oracle/oraclejet-testing/UNSAFE_TestAdapter';
export declare class LineAreaChartAdapter extends TestAdapter {
    /**
     * Triggers selection on the given item
     * @param itemId
     * @param replace
     * @param selectionMode
     */
    doSelectItem(itemId: string | number, replace: boolean, selectionMode: string): Promise<void>;
    /**
     * Triggers item drill on then item with given id.
     * @returns
     */
    doDrillItem(itemId: string | number): Promise<void>;
    doDrillGroup(groupId: string): Promise<void>;
    doZoom(direction: 'in' | 'out'): Promise<void>;
    doPan(direction: 'start' | 'end'): Promise<void>;
}
