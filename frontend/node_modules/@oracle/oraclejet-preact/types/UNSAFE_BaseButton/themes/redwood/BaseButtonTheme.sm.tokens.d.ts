export declare const scaleSchemeSm: {
    borderRadius: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    fontSize: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    sizes: {
        xs: {
            textToEdgePadding: string;
            height: string;
        };
        sm: {
            textToEdgePadding: string;
            height: string;
        };
        md: {
            textToEdgePadding: string;
            height: string;
        };
        lg: {
            textToEdgePadding: string;
            height: string;
        };
    };
};
