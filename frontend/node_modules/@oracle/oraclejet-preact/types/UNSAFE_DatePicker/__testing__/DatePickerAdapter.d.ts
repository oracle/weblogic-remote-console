import { TestAdapter } from '@oracle/oraclejet-testing/UNSAFE_TestAdapter';
import { type CalendarDateRequired } from "../../utils/UNSAFE_calendarDateUtils";
export declare class DatePickerAdapter extends TestAdapter {
    /**
     * Change and commit the value of the component.
     * @param value {CalendarDateRequired} The new value.
     * @returns Promise<void>
     */
    changeValue(value: CalendarDateRequired): Promise<void>;
}
