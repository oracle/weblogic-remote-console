import { TestIdProps } from "../hooks/UNSAFE_useTestId";
type Props = TestIdProps & {
    isMaxLengthExceeded: boolean;
    maxLength: number;
    valueLength?: number;
};
export declare function MaxLengthLiveRegion({ isMaxLengthExceeded, maxLength, testId, valueLength }: Props): import("preact").JSX.Element;
export {};
