import { ComponentChildren, Ref } from 'preact';
type VariantProps = {
    variant?: never;
    resize?: never;
} | {
    variant: 'textarea';
    resize?: 'horizontal' | 'vertical' | 'both';
};
type Props = VariantProps & {
    mainContent?: ComponentChildren;
    insideLabel?: ComponentChildren;
    endContent?: ComponentChildren;
    rootRef?: Ref<HTMLDivElement>;
    startContent?: ComponentChildren;
    hasZeroStartMargin?: boolean;
    labelText?: string;
};
export declare const ReadonlyTextFieldContent: ({ variant, insideLabel, labelText, mainContent, endContent, rootRef, resize, startContent }: Props) => import("preact").JSX.Element;
export type { VariantProps };
