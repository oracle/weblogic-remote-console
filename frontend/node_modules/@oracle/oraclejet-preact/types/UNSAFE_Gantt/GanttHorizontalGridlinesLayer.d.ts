type Props = {
    width: number;
    height: number;
    positions: number[];
};
export declare function GanttHorizontalGridlinesLayer({ width, height, positions }: Props): import("preact").JSX.Element;
export {};
