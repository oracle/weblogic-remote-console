import { meterCircleVars as meterCircleVars } from './MeterCircleContract.css';
import { ComponentThemeType, VariantOptions } from "../../UNSAFE_Theme";
type MeterCircleVariants = typeof variants;
type MeterCircleVariantOptions = VariantOptions<MeterCircleVariants>;
type MeterCircleStyles = typeof styles;
type MeterCircleTheme = ComponentThemeType<MeterCircleVariants, MeterCircleStyles>;
/*******************
 * Component Styles
 *******************/
declare const baseStyle: string;
/*******************
 * Style Variants
 *******************/
declare const variants: {
    readonly size: {
        readonly sm: {
            readonly vars: {
                readonly [meterCircleVars.size]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        readonly md: {
            readonly vars: {
                readonly [meterCircleVars.size]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        readonly lg: {
            readonly vars: {
                readonly [meterCircleVars.size]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        readonly fit: {
            readonly vars: {
                readonly [meterCircleVars.size]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
    };
    readonly trackSize: {
        readonly sm: {
            readonly vars: {
                readonly [meterCircleVars.trackSize]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        readonly md: {
            readonly vars: {
                readonly [meterCircleVars.trackSize]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        readonly lg: {
            readonly vars: {
                readonly [meterCircleVars.trackSize]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        readonly fit: {
            readonly vars: {
                readonly [meterCircleVars.trackSize]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
    };
    readonly interaction: {
        readonly isInteractive: string;
    };
};
declare const styles: {
    centerDivLtr: string;
    centerDivRtl: string;
    fullStyle: string;
    topStyle: string;
    bottomStyle: string;
    rightStyle: string;
    leftStyle: string;
    topFitSmStyle: string;
    topFitLgStyle: string;
    bottomFitSmStyle: string;
    bottomFitLgStyle: string;
    leftFitSmStyle: string;
    leftFitLgStyle: string;
    rightFitSmStyle: string;
    rightFitLgStyle: string;
    fullFitSmStyle: string;
    fullFitLgStyle: string;
    referenceLineLabelContainerStyle: string;
    meterCircleHorizontalSizeStyle: string;
    meterCircleVerticalSizeStyle: string;
    meterCircleFullSizeStyle: string;
    meterCircleCenterContent: string;
    circleWrapperBaseStyle: string;
    interactiveStyle: string;
    circleTrackBaseStyle: string;
    circleValueBaseStyle: string;
    circleValuefullStyle: string;
    circleValuetopStyle: string;
    circleValuerightStyle: string;
    circleValueleftStyle: string;
    circleValuebottomStyle: string;
    referenceBaseLineStyle: string;
    referenceBorderStyle: string;
    textAlignCenter: string;
    textAlignInitial: string;
};
export type { MeterCircleVariantOptions, MeterCircleStyles, MeterCircleTheme };
export { variants, baseStyle, styles };
