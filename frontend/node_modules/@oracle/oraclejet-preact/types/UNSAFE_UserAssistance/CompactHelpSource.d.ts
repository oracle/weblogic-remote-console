type Props = {
    children?: string;
    id?: string;
    labelEdge?: 'inside' | 'start' | 'top';
    source?: string;
};
export declare function CompactHelpSource({ children, id, labelEdge, source }: Props): import("preact").JSX.Element;
export {};
