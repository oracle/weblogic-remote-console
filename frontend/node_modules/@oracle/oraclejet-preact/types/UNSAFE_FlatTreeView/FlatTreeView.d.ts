import { FlattenedTreeNode, FlatTreeViewProps as Props } from './TreeView.types';
import { SelectionRangeDetail } from "../hooks/PRIVATE_useSelection";
/**
 * A FlatTreeView displays flattened data as a tree with highly interactive features.
 */
export declare function FlatTreeView<K extends string | number, D>({ 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledBy, 'aria-describedby': ariaDescribedBy, data, selectionMode, selectedKeys, onSelectionChange, onToggle, viewportConfig, ...rest }: Props<K, D>): import("preact").JSX.Element;
/**
 * A helper function to handle range selection
 */
export declare const handleSelectionRange: <K, D>(detail: SelectionRangeDetail<K>, data: FlattenedTreeNode<K, D>[]) => K[] | {
    offset: number;
    count: number;
};
