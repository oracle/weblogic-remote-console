import { ComponentChildren } from 'preact';
import { ToggleDetail, ListItemRendererContext } from "../UNSAFE_Collection";
type Props<K, D> = {
    children: (context: ListItemRendererContext<K, D>) => ComponentChildren;
    itemKey: K;
    depth: number;
    context: ListItemRendererContext<K, D>;
    isExpanded: boolean | undefined;
    onToggle: (detail: ToggleDetail<K>) => void;
    selected: boolean | 'partial';
    isSelectable: boolean;
    isCheckboxless: boolean;
    isTabbable: boolean;
    isLoading?: boolean;
    isFocusRingVisible: boolean;
};
export declare function TreeItem<K extends string | number, D>({ itemKey, depth, context, children, isExpanded, onToggle, selected, isSelectable, isCheckboxless, isTabbable, isLoading, isFocusRingVisible }: Props<K, D>): import("preact").JSX.Element;
export declare const isMobile: () => boolean;
export declare const calcIndentation: (depth: number, isExpanded?: boolean) => string;
export {};
