import { ComponentChildren } from 'preact';
import type { States, LinkRendererContext, DiagramLinkData, DiagramLayoutOutput, Bounds } from './diagram.types';
type Props<K2, K1, D2, L> = {
    links: D2[];
    states: States<K1, K2>;
    previousStates: States<K1, K2>;
    linkRenderer: (context: LinkRendererContext<D2, L>) => ComponentChildren;
    width: number;
    height: number;
    supportsSelection: boolean;
    layoutOutput: L;
    combinedNodeBounds: Bounds;
};
export declare function DiagramLinkLayer<K2 extends string | number, K1 extends string | number, D2 extends DiagramLinkData<K2, K1> = DiagramLinkData<K2, K1>, L extends DiagramLayoutOutput<K1, K2> = DiagramLayoutOutput<K1, K2>>({ links, states, previousStates, linkRenderer, width, height, supportsSelection, layoutOutput, combinedNodeBounds }: Props<K2, K1, D2, L>): import("preact").JSX.Element;
export {};
