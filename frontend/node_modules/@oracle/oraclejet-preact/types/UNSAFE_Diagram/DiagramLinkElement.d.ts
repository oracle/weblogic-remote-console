import type { State, LinkRendererContext } from './diagram.types';
import { ComponentChildren } from 'preact';
/**
 * Props for Link
 */
export type LinkProps<K2, D2, L> = {
    id: K2;
    label?: string;
    state: State;
    previousState: State;
    isDimmed?: boolean;
    activeId?: string;
    accessibleLabel?: string;
    data: D2;
    linkRenderer: (context: LinkRendererContext<D2, L>) => ComponentChildren;
    supportsSelection: boolean;
    layoutOutput: L;
};
export declare const DiagramLinkElement: <K2, D2, L>({ id, state, previousState, activeId, data, linkRenderer, accessibleLabel, isDimmed, supportsSelection, layoutOutput }: LinkProps<K2, D2, L>) => import("preact").JSX.Element;
