/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
import { LegendItemData } from "../utils/UNSAFE_visTypes/legend";
import { Font, TextDimensions } from "../hooks/PRIVATE_useTextDimensions";
type Props<K> = {
    width: number;
    getTextDimensions?: ((textString: string, font?: Font | undefined) => TextDimensions) | undefined;
    resolvedVars: Record<string, any>;
    orientation?: 'vertical' | 'horizontal';
    legendFontStylesObj?: Font;
    symbolWidth?: number;
    symbolHeight?: number;
    items: LegendItemData<K>[];
};
export type LayoutInfo = {
    totalWidth: number;
    maxItemWidth: number;
    maxItemHeight: number;
    itemsPerRow: number;
    isWrapped?: boolean;
};
export declare const getLinearLegendLayoutInfo: <K>({ width, getTextDimensions, resolvedVars, orientation, legendFontStylesObj, symbolWidth, items }: Props<K>) => {
    totalWidth: number;
    maxItemWidth: number;
    maxItemHeight: number;
    itemsPerRow: number;
    isWrapped: boolean;
} | undefined;
export declare function _getPreferredSize<K>(_width: number, _height: number, items: LegendItemData<K>[], resolvedVars: Record<string, any>, orientation: 'vertical' | 'horizontal', legendFontStylesObj: Record<string, any>, symbolWidth?: number, symbolHeight?: number, getTextDimensions?: (textString: string, font?: Font | undefined) => TextDimensions): {
    width: number;
    height: number;
} | undefined;
export {};
