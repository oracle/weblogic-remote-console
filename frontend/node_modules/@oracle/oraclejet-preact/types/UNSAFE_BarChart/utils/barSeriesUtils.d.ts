/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * Licensed under The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
import { BarItemData } from "../barChart.types";
import { ChartSeriesData } from "../../utils/UNSAFE_visTypes/chart";
/**
 * The function that calculates the barWidth and offset from group center for each series.
 * @param axisStepWidth The width of the group.
 * @param noOfSeries The number of series present in the chart.
 * @param barGapRatio The ratio of gap to axisStepWidth.
 * @param dataItemGaps The ratio that determines the gap between the bars.
 */
export declare function getBarGapsInfo<K extends string | number, D extends BarItemData<K>>(seriesIndex: number, groupIndex: number, series: ChartSeriesData<string | number, any>, axisStepWidth: number, averageGroupZ: number, getDataItem: (seriesIndex: number, groupIndex: number) => D | undefined, barGapRatio: number, numSeriesY: number, numSeriesY2: number, dataItemGaps: number, isStacked: boolean, isRtl: boolean, isHoriz: boolean): {
    barWidth: number;
    offset: number;
    itemsGaps: number;
};
/**
 * Returns the bar gap ratio.
 * @param isStacked Whether the chart is a stacked bar chart.
 * @param startIndex The viewport start index.
 * @param endIndex The viewport end index.
 */
export declare function getBarGapRatio(isStacked: boolean, startIndex: number, endIndex: number): number;
export declare function getBarWidth<K extends string | number, D extends BarItemData<K>>(seriesIndex: number, groupIndex: number, axisStepWidth: number, averageGroupZ: number, getDataItem: (seriesIndex: number, groupIndex: number) => D | undefined): number;
export declare function getOffset<K extends string | number, D extends BarItemData<K>>(seriesIndex: number, groupIndex: number, getDataItem: (seriesIndex: number, groupIndex: number) => D | undefined, averageGroupZ: number, axisStepWidth: number, barWidth: number, numSeries: number, seriesStart: number, center: number, isRtl: boolean, isHoriz: boolean, isVariableBarWidth: boolean): number;
export declare function getWidthWeightVal<K extends string | number, D extends BarItemData<K>>(getDataItem: (seriesIndex: number, groupIndex: number) => D | undefined, seriesIndex: number, groupIndex: number, defaultVal: number): number;
