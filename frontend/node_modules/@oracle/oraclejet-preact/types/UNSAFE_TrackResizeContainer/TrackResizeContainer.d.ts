import type { ComponentChildren, JSX } from 'preact';
import type { UnpackSignals } from "../PRIVATE_oraclejet-internal-utilities/attributeUtils";
import { Size } from "../utils/UNSAFE_size";
type Props = {
    children: (width: number, height: number) => ComponentChildren;
    width?: Size | 'initial';
    height?: Size | 'initial';
} & Pick<UnpackSignals<JSX.IntrinsicElements['div']>, 'class'>;
declare const TrackResizeContainer: ({ children, width: w, height: h, class: className }: Props) => JSX.Element;
export { TrackResizeContainer };
