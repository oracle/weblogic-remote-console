import { ComponentThemeType } from '../../UNSAFE_Theme';
type ColorPickerStyles = typeof styles;
type ColorPickerTheme = ComponentThemeType<ColorPickerStyles>;
/*******************
 * Component Styles
 *******************/
declare const styles: {
    readonly base: {
        readonly maxHeight: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
        readonly width: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
        readonly backgroundColor: string;
        readonly minWidth: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    };
};
export type { ColorPickerTheme, ColorPickerStyles };
export { styles };
