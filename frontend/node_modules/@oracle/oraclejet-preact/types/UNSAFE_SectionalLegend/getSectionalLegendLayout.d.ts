/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
import { RefObject } from 'preact';
import { LegendItemData, LegendSectionData } from "../utils/UNSAFE_visTypes/legend";
import { Font, TextDimensions } from "../hooks/PRIVATE_useTextDimensions";
type Props<K, D> = {
    width: number;
    getTextDimensions?: ((textString: string, font?: Font | undefined) => TextDimensions) | undefined;
    resolvedVars: Record<string, any>;
    rootRef: RefObject<HTMLDivElement>;
    orientation?: 'vertical' | 'horizontal';
    legendFontStylesObj?: Font;
    symbolWidth?: number;
    symbolHeight?: number;
    sections: LegendSectionData<K, D>[];
};
export type LayoutInfo = {
    totalWidth: number;
    maxItemWidth: number;
    maxItemHeight: number;
    itemsPerRow?: number;
    isWrapped?: boolean;
    isSectionWrappedArray?: boolean[];
    isLegendWrappedArray?: boolean[];
    itemsPerRowArray: number[];
};
export declare const getSectionalLegendLayoutInfo: <K, D extends LegendItemData<K>>({ width, getTextDimensions, resolvedVars, rootRef, orientation, legendFontStylesObj, symbolWidth, symbolHeight, sections }: Props<K, D>) => {
    maxRowWidth: number;
    legendHeight: number;
    totalWidth: number;
    maxItemWidth: number;
    maxItemHeight: number;
    itemsPerRowArray: number[];
    isSectionWrappedArray: boolean[];
    isLegendWrappedArray: boolean[];
} | undefined;
export {};
