type Props = {
    maxLength: number;
    valueLength?: number;
    onClick?: () => void;
};
export declare function MaxLengthCounter({ maxLength, valueLength, onClick }: Props): import("preact").JSX.Element;
export {};
