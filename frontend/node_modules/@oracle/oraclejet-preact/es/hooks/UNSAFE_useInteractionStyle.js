/* @oracle/oraclejet-preact: undefined */
export { u as useInteractionStyle } from '../useInteractionStyle-99e32c23.js';
import '../mergeProps-88ea8306.js';
import '../clientHints-a42b510c.js';
import '../useHover-85077adb.js';
import '../useToggle-8b7fcefe.js';
import 'preact/hooks';
import '../useActive-fb6d4d52.js';

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
/**
 * Collection of helper functions for writing your component styles in our theming system.
 * Pass in your style and it will return an object with the appropriate CSS selector wrapping it.
 */
const wrapWithActiveSelector = (style) => ({
    '&:active': style
});
const wrapWithHoverSelector = (style) => ({
    '&:hover': style
});
const wrapWithFocusSelector = (style) => ({
    '&:focus': style
});
const wrapWithFocusVisibleSelector = (style) => ({
    '&:focus-visible': style
});
const wrapWithVisitedSelector = (style) => ({
    '&:visited': style
});
const wrapWithNotActiveSelector = (style) => ({
    '&:not(:active)': style
});
const wrapWithPseudoHoverNotActiveSelector = (style) => ({
    '@media': {
        '(hover: hover)': {
            selectors: {
                '&:hover:not(:active)': style
            }
        }
    }
});
const wrapWithParentSelector = (style, parent) => ({
    [`${parent} > &`]: style
});
const wrapWithParentPseudoHoverSelector = (style, parent) => ({
    '@media': {
        '(hover: hover)': {
            selectors: {
                [`${parent}:hover > &`]: style
            }
        }
    }
});

export { wrapWithActiveSelector, wrapWithFocusSelector, wrapWithFocusVisibleSelector, wrapWithHoverSelector, wrapWithNotActiveSelector, wrapWithParentPseudoHoverSelector, wrapWithParentSelector, wrapWithPseudoHoverNotActiveSelector, wrapWithVisitedSelector };
//# sourceMappingURL=UNSAFE_useInteractionStyle.js.map
