/* @oracle/oraclejet-preact: undefined */
import { useMemo } from 'preact/hooks';

/**
 * A custom hook to determine props to set FormFieldContext
 */
function useFormFieldContextProps({ hasTruncatedLabel, hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly }) {
    return useMemo(() => {
        return isReadonly
            ? {
                hasTruncatedLabel,
                hasValue,
                isFocused,
                isInputFocused,
                isLoading,
                isReadonly: true
            }
            : isDisabled
                ? {
                    hasTruncatedLabel: false,
                    hasValue,
                    isDisabled: true,
                    isLoading
                }
                : {
                    hasTruncatedLabel,
                    hasValue,
                    isFocused,
                    isInputFocused,
                    isLoading
                };
    }, [hasTruncatedLabel, hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly]);
}

export { useFormFieldContextProps as u };
//# sourceMappingURL=useFormFieldContextProps-31bbc12f.js.map
