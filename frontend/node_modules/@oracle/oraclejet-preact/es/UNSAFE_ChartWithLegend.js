/* @oracle/oraclejet-preact: undefined */
import { jsxs } from 'preact/jsx-runtime';

function getFlexDirection(position, isRtl) {
    if (position === 'top') {
        return 'column-reverse';
    }
    else if (position === 'bottom') {
        return 'column';
    }
    else if (position === 'start') {
        return isRtl ? 'row' : 'row-reverse';
    }
    else {
        return isRtl ? 'row-reverse' : 'row';
    }
}

/**
 * Component responsible for laying out the legend around a chart.
 */
function ChartWithLegend({ chart, legend, position, isRtl }) {
    const flexDirection = getFlexDirection(position, isRtl);
    return (jsxs("div", { style: {
            display: 'flex',
            width: '100%',
            height: '100%',
            flexDirection: flexDirection
        }, children: [chart, legend] }));
}

export { ChartWithLegend };
//# sourceMappingURL=UNSAFE_ChartWithLegend.js.map
