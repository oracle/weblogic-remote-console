/* @oracle/oraclejet-preact: undefined */
import { jsx } from 'preact/jsx-runtime';
import { forwardRef, useRef, useImperativeHandle } from 'preact/compat';
import { u as useBareButton } from './useBareButton-07a3eef9.js';
import { U as UNSAFE_DOM_ACCESS } from './unsafeDomAccess-453eb74e.js';

/**
 * BareButtons are unformatted bare buttons that direct users to initiate or take actions and work with a single tap, click, or keystroke.
 */
const BareButton = forwardRef((props, ref) => {
    const rootRef = useRef(null);
    useImperativeHandle(ref, () => {
        return {
            focus: () => {
                rootRef.current?.focus();
            },
            blur: () => {
                rootRef.current?.blur();
            },
            click: () => {
                rootRef.current?.click();
            },
            [UNSAFE_DOM_ACCESS]: rootRef.current
        };
    }, []);
    const { style: buttonStyle, classNames: buttonClass, ...rest } = { ...props };
    const buttonProps = useBareButton({ ...rest });
    const ElementType = props.elementDetails ? props.elementDetails.type : 'button';
    return (jsx(ElementType, { ref: rootRef, style: buttonStyle, class: buttonClass, ...buttonProps, children: props.children }));
});
BareButton.displayName = 'BareButton';

export { BareButton as B };
//# sourceMappingURL=BareButton-0954f33d.js.map
