/* @oracle/oraclejet-preact: undefined */
import { InputTimeMaskAdapter } from './InputTimeMaskAdapter.js';
import '@oracle/oraclejet-testing/UNSAFE_TestAdapter';
import '@oracle/oraclejet-testing/UNSAFE_Driver';
import '@oracle/oraclejet-testing/UNSAFE_Locators';

/**
 * Get the test adapter for the Preact InputTimeMask, identified by the given locator.
 * If a driverOrElement is given, the locator will be searched from there.
 * @param locator The ElementLocator used to search for the InputTimeMask
 * @param driverOrElement The optional TestDriver or TestElement from which to search. If
 * TestElement is given, locator will search underneath that element. If TestDriver is given
 * or the parameter is undefined, the DOM is searched globally.
 * @returns The test adapter for InputTimeMask
 */
function findInputTimeMask(locator, driverOrElement) {
    return new InputTimeMaskAdapter(locator, driverOrElement);
}

export { findInputTimeMask };
//# sourceMappingURL=index.js.map
