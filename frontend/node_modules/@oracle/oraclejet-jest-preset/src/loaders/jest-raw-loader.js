// Copied from npm/jest-raw-loader(v1.0.1) because it doesn't conform to jest 28.x loader requirement
module.exports = {
  process: content => ({ code: "module.exports = " + JSON.stringify(content) })
};
