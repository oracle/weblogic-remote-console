# Copyright (c) 2020, 2021, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.
properties:

- name: ClusterListenPort
  helpSummaryHTML: >
    <p>The listen port of the Coherence Cluster which is acting as the local participant.</p>

# Hide this name.  It confuses the parser, because we really want:
# CoherenceClusterParamsBean.getCoherenceClusterWellKnownAddresses().getCoherenceClusterWellKnownAddresses()
# rather than CoherenceClusterParamsBean.getCoherenceClusterWellKnownAddresses().
- name: CoherenceClusterWellKnownAddressesBean
  source: CoherenceClusterWellKnownAddresses
